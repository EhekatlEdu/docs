# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Overview

Documentation repository for EhekatlEdu (`github.com/EhekatlEdu/docs`). Contains markdown documents and images organized by topic.

## Directory Structure

- `convivios/` — Event/gathering documentation (markdown files + supporting images)
- `proyectos/` — Project-specific documentation

## Guidelines

- All content is written in markdown (`.md`).
- Images should be placed alongside their corresponding markdown files.
- There is no build system, test suite, or linting — this is a plain documentation repo.